import { EmptyState } from '../../components/EmptyState';

export default function LifecycleHealthPage() {
  return (
    <EmptyState
      title="Lifecycle Health"
      description="Cohort-level engagement, drop-off, and moments-of-relevance. The always-on agent's report card."
    />
  );
}
