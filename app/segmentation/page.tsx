import { EmptyState } from '../../components/EmptyState';

export default function SegmentationPage() {
  return (
    <EmptyState
      title="Segmentation + Campaign"
      description="Natural-language segment building across Dynamics + AEP. Send to AEP, draft AJO campaign, lookalike expansion."
    />
  );
}
