# Anthropic API — not used in scaffolding phase, wired later
ANTHROPIC_API_KEY=

# Set to true to bypass the canned agent stream and use a real Claude Agent SDK call
# (not implemented yet)
USE_REAL_AGENT=false
